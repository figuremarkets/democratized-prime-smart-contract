{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ExtendedTokenInfoResponse",
  "description": "Extensions to the CW20 `token_info` response.",
  "type": "object",
  "required": [
    "decimals",
    "name",
    "symbol",
    "total_scaled_supply",
    "total_supply"
  ],
  "properties": {
    "decimals": {
      "description": "The decimal precision of the token.",
      "type": "integer",
      "format": "uint8",
      "minimum": 0.0
    },
    "name": {
      "description": "The name of the token.",
      "type": "string"
    },
    "symbol": {
      "description": "The symbol of the token.",
      "type": "string"
    },
    "total_scaled_supply": {
      "description": "The total supply of the token in scaled units.",
      "allOf": [
        {
          "$ref": "#/definitions/Uint128"
        }
      ]
    },
    "total_supply": {
      "description": "The total supply of the token. If the contract has a configured pool address, the total supply will be returned in underlying units, otherwise scaled units.",
      "allOf": [
        {
          "$ref": "#/definitions/Uint128"
        }
      ]
    }
  },
  "additionalProperties": false,
  "definitions": {
    "Uint128": {
      "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
      "type": "string"
    }
  }
}
